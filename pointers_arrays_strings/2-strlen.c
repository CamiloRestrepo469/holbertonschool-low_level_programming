#include "main.h"
#include <string.h>
/**
 * _strlen - a function that returns
 *  the length of a string
 *  @s: save values add
 *
 *  Return: 0.
 */

int _strlen(char *s)
{
int i, l=0;
for (i=0; s[i] != '\0'; i++)
{
l++
}	
return (i);
}
